# Engineering Forge — the MVP (`game/`) is 100% client-side: no backend, no
# database, no API keys. Progress is stored in the browser's localStorage.
# This file intentionally has no variables — see docs/02_ARCHITECTURE.md and
# docs/03_DECISIONS.md (DEC-010-004) for why.
