---
idea: IDEA-010
doc: 03_DECISIONS
version: 0.15.0
standard: GUILD-DOC-STANDARD@0.2.0
status: draft
updated: 2026-07-29
linear: https://linear.app/engineering-guild/project/idea-010-engineering-forge-jogo-educacional-4f3eb9ffca14
---

# Decisões (append-only)

| ID | Data | Decisão | Contexto/Consequência |
|---|---|---|---|
| DEC-010-001 | 2026-06-10 | Adotar GUILD-DOC-STANDARD@0.1.0 | Estrutura docs/ padronizada |
| DEC-010-002 | 2026-06-10 | Slot IDEA-010 = Engineering Forge | Resolvida a duplicação anterior (010 era "projeto money" = 006) |
| DEC-010-003 | 2026-07-19 | Conceito de produto = **"A Forja"**: aprendiz forja artefatos de engenharia funcionais, testados por simulação real, organizados em "packs" por disciplina; primeiro pack = Estruturas (pontes) | Síntese entre a ambição do GDD 2025 (simulação física, progressão), a identidade "forja" dos mockups originais, e o escopo enxuto exigido pelo L1. Substitui as três visões incompatíveis anteriores (universidade blockchain / ferreiro literal / clicker genérico). **Proposto por sessão de agente — pendente de validação por Caio.** |
| DEC-010-004 | 2026-07-19 | Reescrever do zero em `game/` (Vite+React+TS+Zustand, client-side, sem backend); arquivar `engineering-forge-v1` e `engineering-forge-docs` em `legacy/`; descartar blockchain/NFT/MongoDB do escopo do MVP | Fecha a decisão pendente desde o L1 (reformar vs. reescrever). O código legado era um clicker sem relação com a visão de produto e uma camada MongoDB nunca conectada à UI; reescrever é mais barato que desacoplar. Blockchain permanece como possível diferencial futuro, não MVP. |
| DEC-010-005 | 2026-07-19 | Tier GOX = **T1 (Standard)** | Produto do portfólio com potencial de ir a público/funil, não mission-critical. Requer charter + gate de qualidade antes de "Operational" (GOX `01_GRADED_TIERS.md`). |
| DEC-010-006 | 2026-07-19 | UI do jogo em **inglês**, com estrutura de strings pronta para i18n (PT-BR depois); docs continuam em PT-BR | Produto vendável globalmente desde o início; docs seguem o padrão do repo (PT-BR conforme precedente local, embora o CLAUDE.md da hub DEC-001-008 padronize inglês para docs da Guild — conflito a esclarecer com Caio; docs de produto seguindo o precedente do próprio IDEA-010). |
| DEC-010-007 | 2026-07-19 | Credencial MongoDB Atlas hardcoded encontrada em `legacy/engineering-forge-v1` (4 arquivos) foi **removida do código-fonte** e substituída por leitura de variável de ambiente | **Ação pendente e urgente para Caio (HITL):** a credencial já esteve exposta em texto plano num repositório público e permanece no histórico do git — rotacionar a senha/usuário no MongoDB Atlas imediatamente, independentemente da remoção do código. Ver `06_OPERATIONS.md` §Segurança. |
| DEC-010-008 | 2026-07-22 | Segundo pack — **Circuitos** — jogável: tutorial + 2 níveis, motor de análise nodal (`game/src/physics/circuit.ts`) reaproveitando o solver de álgebra linear da treliça | Prova o modelo "packs por disciplina" da DEC-010-003 com um segundo domínio de simulação real (Lei de Ohm/Kirchhoff). Navegação reestruturada: `/` agora é seleção de pack, `/structures` e `/circuits` cada um com sua lista de níveis. Bug de design pego só ao jogar no browser (não pelo type-check): um resistor queimava a si mesmo no nível "Higher Voltage" — corrigido subindo seu rating de potência; ver `02_ARCHITECTURE.md`. |
| DEC-010-009 | 2026-07-24 | Aprofunda os dois packs: material **cable** (tração-only, falha imediata em compressão) + nível 4 "The Tower" em Estruturas; nível 3 "Splitting the Load" (resistores em paralelo) em Circuitos | Trabalho autônomo durante período sem supervisão direta do Caio — decisões de conteúdo/design tomadas por critério próprio, seguindo o padrão de rigor já estabelecido (topologia da torre verificada computacionalmente com o próprio solver antes de fixar números; ambos os níveis jogados ponta a ponta no browser antes de commit). Achado operacional relevante durante esta sessão: o job `deploy` do GitHub Actions falha desde o merge da PR #1 (`404 Project not found` no Cloudflare Pages) — ver `06_OPERATIONS.md`, ação pendente e urgente para Caio antes de qualquer deploy real. |
| DEC-010-010 | 2026-07-24 | Terceiro pack — **Máquinas** — fundação jogável: tutorial "First Drive" ("loadout" de motor+chassi, não canvas espacial), motor de física em cinemática + 2ª lei de Newton + teto de tração (`physics/vehicle.ts`) | Continuação do trabalho autônomo (mesmo contexto da DEC-010-009). Prova o modelo "packs por disciplina" com um terceiro domínio de simulação (mecânica newtoniana, não álgebra linear como os outros dois — decisão deliberada de não forçar a metáfora de canvas numa mecânica que não precisa dela). Escopo enxuto por design (fundação + 1 nível), mais níveis explorando o teto de tração ficam para depois — mesmo padrão incremental das Fases 1 e 2. |
| DEC-010-011 | 2026-07-25 | Máquinas nível 1 "Full Throttle": novo motor Turbo (8000N) exercita o teto de tração de verdade | Continuação do trabalho autônomo. Antes de fixar os números, provou-se por álgebra (não só testado) que neste modelo o chassi leve nunca perde pro pesado em velocidade final — ambos convergem pro mesmo teto de aceleração `μg` quando limitados por tração, e o leve sempre alcança esse teto com motor mais barato. Descartada a ideia original de uma pegadinha "chassi pesado vence por causa do grip" por não ser sustentada pela física; o nível ensina em vez disso que motor mais forte que o teto de tração é dinheiro desperdiçado — ver `04_ROADMAP.md` Fase 4b. |
| DEC-010-012 | 2026-07-25 | Endurecimento para produção: cobertura de teste unitário na camada de scoring/custo dos três packs (`game/build.ts`, `circuitBuild.ts`, `vehicleBuild.ts`), até então sem nenhum teste dedicado (só os solvers de física tinham) | Continuação do trabalho autônomo, priorizando robustez em vez de conteúdo novo nesta rodada. 17 testes novos cobrindo montagem de modelo, cálculo de custo (só peças do jogador, tabuleiro/carga são grátis) e lógica de estrelas. Pegou um erro real na hora de escrever: a asserção inicial de um teste de integração assumia que um combo passaria, mas o orçamento da fixture de teste era baixo demais pro custo real — corrigido antes de commitar. Ver `04_ROADMAP.md` Fase 4c. |
| DEC-010-013 | 2026-07-25 | Corrigido bug de overflow horizontal em telas estreitas (mobile): a linha de seleção de material na `Toolbar` (compartilhada por Estruturas e Circuitos) não quebrava linha | Achado só ao redimensionar o viewport de verdade (375×667, iPhone SE) e medir `scrollWidth` vs `clientWidth` — nenhum type-check ou teste unitário pega isso. Qualquer nível com 3 materiais desbloqueados estourava a tela (ex.: tutorial de Circuitos, 448px vs 375px; nível 4 de Estruturas "The Tower", 419px vs 375px); níveis com só 2 materiais cabiam por sorte. Causa raiz: o contêiner externo da `Toolbar` tinha `flex flex-wrap`, mas o contêiner interno dos botões de material só tinha `flex gap-2` — faltava `flex-wrap` nele, então os botões nunca quebravam linha mesmo a linha inteira podendo. Corrigido com uma classe (`game/src/components/Toolbar.tsx`). Verificado nos 7 fluxos principais + nos dois níveis de 3 materiais, todos sem overflow depois da correção; Máquinas usa um grid `grid-cols-2` (não a `Toolbar`) e nunca teve o problema, por não depender de wrap de linha. Ver `04_ROADMAP.md` Fase 4d. |
| DEC-010-014 | 2026-07-25 | Máquinas nível 2 "Uphill Battle": novo mecanismo de **inclinação** (`inclineDegrees`, opcional, default 0) — rampa de 15°, com um novo estado de falha "estolado" (`stalled`) quando a gravidade ao longo da rampa vence a força que chega na roda | Continuação do trabalho autônomo, priorizando conteúdo nesta rodada (Máquinas tinha só 1 nível além do tutorial, o mais atrasado dos três packs). Verificado por álgebra e depois por script numérico antes de fixar o nível: numa rampa, o teto de tração encolhe por `cos θ` e a gravidade ao longo da rampa (`mg sin θ`) subtrai da força líquida — só motor turbo + chassi leve atinge a meta de 27 m/s (28,4 m/s); motor pequeno em qualquer chassi e motor grande+chassi pesado **estolam** (aceleração ≤ 0, nunca saem do lugar); motor grande+chassi leve e turbo+chassi pesado se movem mas ficam abaixo da meta (13,9 e 25,7 m/s). Generalizado (e testado) o teorema da Fase 4b — "o chassi leve nunca perde pro pesado" — para rampas: o chassi que o motor satura primeiro nunca é superado pelo que ainda não saturou, mesmo em subida. Jogado ponta a ponta no browser nas 5 combinações relevantes antes de commitar, todos os números batendo com o cálculo prévio. Ver `04_ROADMAP.md` Fase 4e, `02_ARCHITECTURE.md` §Máquinas. |
| DEC-010-015 | 2026-07-25 | Passe de acessibilidade no "chrome" da UI (não no canvas): (1) token `--fg-subtle` subiu de `#6b7686` pra `#788393` — o valor vendorizado só batia ~3.8-4.1:1 contra os fundos escuros, abaixo do 4.5:1 exigido pelo WCAG AA pra texto normal, e toda utilização desse token no jogo é `text-xs`/`text-sm`; (2) os três overlays de resultado ganharam `role="dialog"` + `aria-modal="true"` + `aria-labelledby` + foco automático no heading ao abrir, então um usuário de leitor de tela é avisado do resultado assim que ele aparece, não só quem enxerga a tela; (3) o indicador de estrelas (extraído pra um componente `Stars` único, reaproveitado nos 3 overlays — antes era código idêntico triplicado) ganhou `role="img"` + `aria-label` tipo "3 out of 3 stars"; (4) as três telas de seleção de nível (Estruturas/Circuitos/Máquinas — também consolidadas num componente único `LevelSelectList`, mesma motivação de não corrigir a mesma coisa 3x) ganharam texto `sr-only` indicando "locked"/"not yet completed"/"X of 3 stars" pra quem navega por leitor de tela, já que antes um item bloqueado não tinha nenhum sinal não-visual de que estava bloqueado (só o emoji 🔒, que é puramente decorativo pra quem não enxerga) | Continuação do trabalho autônomo. Verificado por cálculo de contraste (fórmula WCAG de luminância relativa, rodado num script, não só "parece ok") antes de escolher o novo tom, e por teste no browser (Playwright, temporário) confirmando: foco visível ao navegar por Tab (o outline padrão do browser já é visível contra o fundo escuro — não precisou de estilo customizado), heading do dialog recebendo foco de fato ao abrir, `aria-label` correto nas estrelas, e texto `sr-only` correto nos itens bloqueados/desbloqueados. **Escopo deliberadamente limitado ao chrome** (botões, textos, contraste, semântica de diálogo) — navegação por teclado no canvas SVG (posicionar/conectar nós da treliça ou do circuito sem mouse) é um problema de design de interação bem maior e diferente, não tentado nesta rodada; ver `04_ROADMAP.md` Fase 4f para o registro explícito dessa lacuna como trabalho futuro, não esquecimento. |
| DEC-010-016 | 2026-07-26 | Circuitos nível 4 "One Path, Two Bulbs": primeira vez com duas cargas dadas no **mesmo caminho de corrente** (em série), não em ramos independentes (nível 1) nem uma única carga (tutorial/nível 2/nível 3) | Continuação do trabalho autônomo. Mecanismo genuinamente novo: as resistências das duas lâmpadas se somam nesse caminho único (3+3=6Ω) — verificado por script numérico antes de fixar `sourceVoltage`/topologia. Resistor pequeno (5Ω), que funcionava no tutorial pra uma lâmpada só, agora estoura as duas (5,58W contra o limite de 4W) — corta o hábito adquirido nos níveis anteriores, forçando o jogador a perceber que a resistência combinada das duas cargas mudou a conta. Resistor grande (15Ω) mantém ambas na faixa segura (1,53W). Descoberta emergente aceita como válida, não um bug: dois resistores pequenos em série (10Ω) também passam (2,64W) e saem mais baratos que o grande sozinho, já que o custo escala só com o comprimento total do fio/resistor, não com o número de segmentos — mesmo espírito de "mais de uma solução física válida" já visto no nível 3 de Estruturas. Jogado ponta a ponta no browser confirmando queima com resistor pequeno e "It lights up!" com 3 estrelas ($32 de $60) usando o grande. Ver `04_ROADMAP.md` Fase 4g, `02_ARCHITECTURE.md` §Circuitos. |
| DEC-010-017 | 2026-07-26 | Máquinas nível 3 "Delivery Run": novo mecanismo de **carga dada** (`payloadMass`, opcional, default 0) — 400kg de carga fixa que soma à massa do chassi tanto pro teto de tração quanto pra F=ma, em pista plana, isolado de inclinação/teto de tração já ensinados | Continuação do trabalho autônomo — Máquinas voltou a ser o pack mais atrasado (3 níveis) depois que Circuitos alcançou Estruturas (5 cada). Primeira vez que Máquinas tem um elemento **dado/fixo** que o jogador acomoda em vez de escolher — mesmo padrão do tabuleiro com carga (Estruturas) e da lâmpada (Circuitos), agora aplicado à massa. Verificado por script numérico antes de fixar os números: motor grande + chassi leve, que passava facilmente sem carga (~27,4 m/s), cai pra ~22,4 m/s com 400kg a bordo — nenhum combo fica limitado por tração nesta pista (o teto de tração com a carga somada, ~9,4kN, fica bem acima da força de qualquer motor do catálogo), isolando a lição só na massa. Só motor turbo + chassi leve atinge a meta de 32 m/s (36,5 m/s); turbo+chassi pesado fica perto mas abaixo (31,6 m/s). Jogado ponta a ponta no browser nas 4 combinações relevantes, todos os números batendo com o cálculo prévio. Ver `04_ROADMAP.md` Fase 4h, `02_ARCHITECTURE.md` §Máquinas. |
| DEC-010-018 | 2026-07-28 | Estruturas nível 5 "The King Post": novo material **compressão-only** (`strut`), espelho do `cable` — falha imediata se ficar em tração, mesma filosofia de simplificação invertida | Continuação do trabalho autônomo. Topologia (viga + pontalete vertical acima do ponto de carga + duas pernas até as ancoragens) verificada rodando o solver antes de fixar materiais/números, mesmo processo do DEC-010-009 (The Tower): as duas pernas ficam em **compressão** (-33.333N cada, boas para strut) e o pontalete vertical fica em **tração** (+40.000N, bom pra cabo) — papéis exatamente opostos das linhas de The Tower, um bom contraste didático. Solução correta (strut nas pernas, cabo no pontalete) custa $64 de $110 (3 estrelas); testado também trocando os materiais de propósito (cabo nas pernas, strut no pontalete) confirmando que cada material falha na direção errada com sua própria mensagem distinta na UI. Ver `04_ROADMAP.md` Fase 4i, `02_ARCHITECTURE.md` §Estruturas. |
| DEC-010-019 | 2026-07-28 | Máquinas nível 4 "The Sweet Spot": novo mecanismo de **janela de velocidade** (`maxSpeed`, opcional) — o primeiro nível de qualquer pack onde "rápido demais" reprova, não só "devagar demais" ou "caro demais" | Continuação do trabalho autônomo. Verificado por script numérico antes de fixar os números: das 6 combinações motor×chassi em pista plana/100m, só motor grande + chassi pesado cai dentro da janela [20,25] m/s (22,36 m/s) — motor pequeno fica abaixo em ambos os chassis, motor grande+leve e motor turbo em qualquer chassi ultrapassam o teto. Versão mais forte da lição do teto de tração: aqui o motor mais forte não é só dinheiro desperdiçado, é a escolha errada. `maxSpeed` omitido preserva o comportamento de todo nível anterior (só piso). Jogado ponta a ponta no browser nas 4 combinações relevantes (devagar demais, dentro da janela com 3 estrelas, duas variações "rápido demais" com mensagem própria), todos os números batendo com o cálculo prévio. Ver `04_ROADMAP.md` Fase 4j, `02_ARCHITECTURE.md` §Máquinas. |
| DEC-010-020 | 2026-07-29 | Circuitos nível 5 "Two Paths, One Bulb": primeira vez que **nenhum resistor sozinho** resolve o nível — o pequeno e o grande deixam a lâmpada fraca demais, exigindo dois caminhos em paralelo pela primeira vez sem restringir quais peças estão disponíveis | Continuação do trabalho autônomo. Achado real de verificação: a primeira tentativa de projetar o nível assumiu a fórmula idealizada de resistores em paralelo (`R1‖R2` puro), mas `circuitStore.ts` recusa uma segunda aresta entre o mesmo par de nós — na prática, colocar "dois resistores em paralelo" sempre exige um nó extra e um segmento de fio a mais em série em cada ramo (e outro fio no caminho de retorno até o terra), o que muda a resistência equivalente real. Rodando o solver de verdade com a topologia exatamente como o jogador precisaria construí-la (não a fórmula idealizada), a lâmpada saiu fraca demais no primeiro cálculo — corrigido reajustando `sourceVoltage` até achar uma faixa onde nenhum resistor sozinho funciona mas dois em paralelo (iguais ou diferentes) funcionam. Solução mais barata (dois resistores pequenos, $41 de $70, 3 estrelas); misturar os dois valores também funciona, só custa mais ($50, 2 estrelas) — mesmo espírito de "mais de uma solução física válida" dos níveis anteriores. Jogado ponta a ponta no browser nas 4 combinações relevantes, todos os números batendo com o cálculo prévio. Ver `04_ROADMAP.md` Fase 4k, `02_ARCHITECTURE.md` §Circuitos. |
